# Session Journal -- 2026-03-15

## Summary

Completed Phase 0 (Foundation) and populated Phase 1 backlog.

## What Was Done

### Phase 0 -- completed in full

**Go project scaffold:**
- Module: `github.com/Chevron7Locked/kima-go`, Go 1.26.1 (installed at `/home/chevron7/go126/`)
- chi v5 router chosen over stdlib 1.22+ mux for middleware ecosystem
- All 14 directories created per requirements doc Section 1.2

**Infrastructure packages:**
- `pkg/config`: `caarlos0/env/v11` typed config, all fields covered, unit tests for defaults/required/invalid/CORS slice/IsProduction
- `pkg/db`: pgxpool connection, golang-migrate runner with embedded SQL from `migrations/` package
- `pkg/cache`: go-redis/v9 connection, `Pinger` wrapper for interface satisfaction
- `pkg/testutil`: `PostgresContainer` (pgvector:pg16) + `RedisContainer` (redis:7-alpine) via testcontainers-go

**Schema migration (`migrations/000001_initial.up.sql`):**
- 21 tables matching requirements doc
- HNSW index on track_embeddings (m=16, ef_construction=64) and vibe_vocabulary (m=16, ef_construction=200)
- GENERATED ALWAYS AS search_vec columns on tracks/albums/artists/podcasts/audiobooks
- music_simple FTS config: unaccent + simple dictionary (no stemming, accent-insensitive)
- Partial index for shadow artist cleanup
- Default quality profile seed

**Schema correctness tests (9 sub-tests):**
All 21 tables exist, extensions enabled (pg_trgm/vector/unaccent), music_simple FTS config, HNSW index present, GIN search_vec indexes, partial shadow index, CHECK constraint enforcement, UNIQUE constraint enforcement, GENERATED column output, migration idempotency.

**HTTP layer:**
- `/health` (liveness) and `/health/ready` (readiness) with consumer-defined `DBPinger`/`CachePinger` interfaces
- Health tests cover all 4 dep-state combinations + Content-Type
- CORS middleware with Vary header, preflight handling
- Rate limiting middleware with per-IP buckets (tested: burst exhaustion -> 429, separate IP isolation)
- Logger middleware capturing status codes

**Enforcement:**
- `.golangci.yml`: errcheck, govet (shadow), staticcheck, depguard (full import boundary rules), gochecknoglobals, gochecknoinits, funlen (60/40), gocognit (15), wrapcheck, exhaustive, testifylint, and more
- `scripts/check-structure.sh`: 400/600-line file limits, api/v1 80-line limit, junk-drawer name detection, single-file package enforcement
- CI: 2 jobs (Lint + Test), race detector, "every package must have tests" check
- GitHub repo + branch ruleset: both jobs required before merge, admin bypass

### Phase 1 -- backlog populated

Read requirements doc Sections 5 (Library), 7 (Streaming), 12 (Auth), 13 (Subsonic). Populated 21 tasks across 5 groups: Auth, Scanner, Library API, Streaming, Subsonic.

**Deferred explicitly:**
- Radio mode (Section 7.2.6) -- needs relationship graph, Phase 3
- Play event signals (EWMA, transition graph) -- Phase 3
- External cover art fallbacks (Deezer/MB CAA/LastFM) -- Phase 4
- File watching (fsnotify) -- Phase 3
- OurSpace flow -- Phase 6 (column in schema already)

## Decisions Made

- **Test philosophy**: tests must fail if the behavior they describe is wrong. No smoke tests.
- **Consumer-defined interfaces everywhere**: enables error-path testing without containers.
- **Structural enforcement**: custom script + golangci-lint together. No lll/gocyclo/dupl/goconst.
- **migrations/ package**: dedicated package at project root for `//go:embed *.sql` (cannot embed outside package dir).

## Surprises

- Go not installed -- had to install twice (1.24.3 then 1.26.1) because module dependencies required Go 1.25+.
- Module path rename left stale imports in 5 files -- fixed file by file.
- Health handler needed interface refactor for error-path tests.
- GitHub push blocked by ruleset after creation -- needed admin bypass actor.

## Next Steps

Phase 1, starting with sqlc setup then auth service. First task: configure sqlc.yaml and write SQL queries for all library entities.
